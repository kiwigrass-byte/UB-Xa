  local rows, cols = 72, 18
  local tempMatrix = {} -- store the reshaped split-double data
  for r = 1, rows do
    tempMatrix[r] = {}
    for c = 1, cols do
        local index = (r - 1) * cols + c
        tempMatrix[r][c] = data[index]
    end
  end
  local idx = {1,2,3,4,5,6,7,10,12,13,14,15} -- 12 columns in sdMatrix that are parameters
  sdMatrix = {}
--  if sdMatrix == nil then return end
  for r = 1, rows do
    for i = 1, #idx do
      sdMatrix[i] = tempMatrix[r][idx[i]]
    end
  end
